# 웹 백엔드(담다 API) 배포용 의존성 — AI/torch 제외 (AI는 별도 레포)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
email-validator>=2.0.0

# DB
sqlalchemy>=2.0.0
alembic>=1.13.0
pymysql>=1.1.0
cryptography>=42.0.0

# 외부 HTTP (날씨/소셜 로그인)
httpx>=0.27.0

# 인증
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# PDF 리포트 생성 (H.7.1) — 시스템 패키지(Pango/Cairo/GDK-Pixbuf)는 Dockerfile 참고
weasyprint>=69.0
