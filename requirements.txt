# BE/ 의존성
# 새 venv 에서: pip install -r requirements.txt 하나로 끝나야 함.
# CPU 추론 기준. GPU 쓰려면 torch/torchvision 은 https://pytorch.org/get-started 참고해서 별도 설치.

# --- 웹 서버 ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6     # UploadFile 처리용
pydantic>=2.0.0

# --- 이미지/설정 ---
Pillow>=10.0.0
PyYAML>=6.0

# --- 외부 HTTP ---
httpx>=0.27.0               # ESP32-CAM HTTP 호출용 (/api/measure)
requests>=2.31.0            # 식약처 API 호출용 (scripts/products/2a_fetch_kfda_all.py)

# --- AI 추론 (src.infer import 됨) ---
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
pandas>=2.0.0               # scripts/products/ 파이프라인용

# --- DB / 웹 백엔드 (담다 API) ---
sqlalchemy>=2.0.0
alembic>=1.13.0
pymysql>=1.1.0              # MySQL 드라이버
cryptography>=42.0.0        # pymysql 인증(caching_sha2)용
pydantic-settings>=2.0.0   # .env 로드
python-dotenv>=1.0.0

# --- 인증 (JWT + 해싱 + 소셜) ---
passlib[bcrypt]>=1.7.4
python-jose[cryptography]>=3.3.0
email-validator>=2.0.0
